services:
  db:
    image: postgres:18-alpine
    container_name: omnifit_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 7Er2zgu526lCAN9
      POSTGRES_DB: omnifit
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  postgres_data:
